"use strict";
//we first find the maximum element and place the maximum element at the end. We repeat the same process for remaining element.
function heapify(array) {
    return array;
}
var ar = [4, 1, 2, 3];
heapify(ar);
