// 2. Find the Square root of an integer – 
// start with O(n) and optimize the solution as much as you can till O(Log n)
// For Input: x = 4 .. Output: 2
// For Input: x = 11 .. Output: 3
// Note the output is always an integer.

const sqrt = (n) =>  Math.floor(Math.sqrt(n));
console.log(sqrt(11))